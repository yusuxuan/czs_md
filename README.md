# 充值神的MD插件（TurboWarp / CCW 自定义扩展）

## 作者

- 充值神

## 代码用法

- 创建/更新MD窗口：输入 Markdown 内容即可渲染到悬浮窗口中
- 换行：用 `\h` 表示换行（渲染前会转换为真正的换行）

## 注意事项

- 本扩展需要在非沙盒（unsandboxed）模式下运行，否则会报错无法使用。
- 如果遇到中文文件名导致加载失败，建议将文件重命名为英文（例如 `chongzhishen-md.js`）再上传，并同步更新 URL。
